#include <stdio.h>
#include <string.h>
int main()
{
char *str1 = "anand kumar";
char *str2 = "anand";
int len = strlen(str1);
int len2 = strlen(str2);
printf("length of the string is %d",len);
printf("length of the string is %d",len2);
return 0 ;
}